# Cardinal Solution

## Getting started

1.  

In the `cardinal-server` folder

   
 * `npm install`
  
 * `nodemon` (if installed) or `node index.js`

2.  

In the `cardinal-client` folder

   
 * `npm install`
  
 * `npm start`
